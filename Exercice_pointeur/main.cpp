#include <iostream>
#include "Exo.h"


int main() {
      char *chaine = "je suis un zombie errant qui mange n'importe quoi";
      char *dest = nullptr;
      dest = new char[20];
      //char *chaine4 = cherche_char(chaine,'B');
      //copie(chaine1, chaine);
      // concatenate(chaine, chaine1);
    supprime_charactere(chaine, 'e');
    //inversion_chaine(chaine);
       //to_upper(chaine);
      //std::cout << compare(chaine,chaine1) << std::endl;



    //std::cout << longueur(chaine1) << std::endl;
      //std::cout << longueur(chaine2) << std::endl;
}
